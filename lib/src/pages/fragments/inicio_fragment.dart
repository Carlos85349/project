import 'package:flutter/material.dart';

class InicioFragment extends StatelessWidget{
  const InicioFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Inicio"));
  }
}